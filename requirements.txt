# Dependências do serviço web (api.py + routers/ + modules/).
#
# Só pin exato onde a versão muda comportamento; faixa no resto. Pin exato de
# transitiva (urllib3, pydantic, certifi) vem de requests e fastapi — travar as
# duas pontas é o que produz ResolutionImpossible no build.
#
# Fora daqui de propósito:
#   MetaTrader5   Windows-only, inerte no servidor
#   ccxt          só o router de trading usava, e ele está desativado
#   scikit-learn  e joblib: usados por treinar_modelo.py e robo_trader.py,
#   joblib        que não fazem parte do serviço web

fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
jinja2>=3.1

# yfinance dita a faixa de pandas/numpy que o pip consegue resolver. Se o build
# reclamar de conflito, é este bloco que cede — nunca pine pandas acima do que
# a sua versão de yfinance aceita.
yfinance>=0.2.60
pandas>=2.2
numpy>=1.26

requests>=2.32
pypdf>=5.1
