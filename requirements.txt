# Versões fixadas a partir do venv de referência (Python 3.13, Windows).
# Sem pin o projeto não é reprodutível: o yfinance troca contrato entre
# releases menores — foi ele que mudou `auto_adjust` para True por padrão em
# yf.download e a convenção de `dividendYield` de fração para pontos
# percentuais. routers/equity.py assume o comportamento da linha 1.x.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
jinja2==3.1.6
python-multipart==0.0.32
python-dotenv==1.2.3

pandas==3.0.5
numpy==2.2.6
scikit-learn==1.9.0
joblib==1.6.0

yfinance==1.7.0
ccxt==4.5.77
requests==2.34.2
urllib3==2.7.0
pypdf==6.18.0
