venv/
__pycache__/
*.env
.env
*.diff
subir.bat
subir_log.txt
inspecionar.bat
inspecao_log.txt
fundos.bat
fundos_log.txt

# cache local da carteira do IBOV (regenerado a cada 12h)
modules/cache_ibov.json

# Preview enviado pelo Claude para conferencia visual — nao faz parte do servico
Claude outputs/
preview_terminal.html

# Arquivo gravado por engano; pode apagar da pasta.
modules/opcoes_tmp_ignore.py

# Chave de assinatura do app Android. Perder = nunca mais atualizar o app.
*.keystore
*.jks
# Artefatos do Bubblewrap
/twa/
*.aab
*.apk

# Banco de contas, sessoes e cotas. Contem hash de senha de usuario real:
# nunca versionar, nunca copiar para fora do servidor.
contas.db
contas.db-wal
contas.db-shm
